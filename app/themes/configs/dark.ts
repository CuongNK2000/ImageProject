import {ThemeApp} from '../../common/type';
import {color} from '../color';

const theme: ThemeApp = {
  id: 2,
  colors: {
    bgTest: '#000',
    textColor: 'black',
    errorText: '#FF4848',
    background: '#F5F5FA',
    backgroundFeature: '#FFFFFF',
    backgroundHeader: color.primary,
    btnOpacity: '#D0E2FB',
    btnColor: color.palette.blue1,
    bgAccount: '#0E55D7',
  },
};

export default theme;
